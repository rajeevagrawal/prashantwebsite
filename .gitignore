# Credentials — never commit
localCredential/credential
localCredential/*.env
*.env
*.key
*.pem

# Wix site mirror (58MB — too large for git)
analyse-littleseedschildrenscenter/site-mirror/

# Claude local settings
.claude/

# OS files
.DS_Store
Thumbs.db

# Node
node_modules/
